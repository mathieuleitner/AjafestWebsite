{
  "kind": "collectionType",
  "collectionName": "lineups",
  "info": {
    "name": "Lineups"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "artists": {
      "via": "lineups",
      "collection": "artist",
      "dominant": true
    },
    "Day": {
      "type": "enumeration",
      "enum": [
        "Samedi",
        "Dimanche"
      ]
    }
  }
}
